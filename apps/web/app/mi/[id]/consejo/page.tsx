"use client";

/**
 * Consejo en el portal del residente/junta.
 * Misma UI que admin (documentos, comentarios); los permisos los impone Convex:
 * junta ve y comenta; solo admin/contadora suben.
 */
export { default } from "@/app/condominio/[id]/consejo/page";
