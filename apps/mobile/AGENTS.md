# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

Project is pinned to SDK 54 (matches the installed Expo Go client). Run `bunx expo install --check`
in `apps/mobile` after touching any `expo-*` dependency to keep versions aligned — don't bump `expo`
itself without also updating every `expo-*` package and re-running that check.
